[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "djobs"
version = "0.3.0"
description = "Crash-proof task memory for AI coding agents — your agent's multi-file work survives IDE crashes."
requires-python = ">=3.11"
license = "MIT"
readme = "README.md"
keywords = ["mcp", "ai-agent", "crash-recovery", "task-queue", "durable", "sqlite", "copilot", "cursor", "cline", "audit-log"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
    "Topic :: System :: Distributed Computing",
]

dependencies = [
    "mcp[cli]>=1.0",
]

[project.urls]
Homepage = "https://github.com/jhuang-tw/djobs"
Repository = "https://github.com/jhuang-tw/djobs"
Issues = "https://github.com/jhuang-tw/djobs/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
pg = [
    "psycopg[binary]>=3.1",
]

[project.scripts]
djobs = "djobs.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/djobs"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
src = ["src", "tests"]
target-version = "py311"
line-length = 99

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]
