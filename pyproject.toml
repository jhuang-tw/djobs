[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "djobs"
version = "0.2.0"
description = "SQLite-backed durable task queue with first-class MCP integration, purpose-built for AI coding agents (crash recovery, audit trail, embedded daemon)."
requires-python = ">=3.11"
license = "MIT"
readme = "README.md"
keywords = ["job-queue", "task-queue", "mcp", "ai-agent", "durable", "sqlite", "crash-recovery", "audit-log"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
    "Topic :: System :: Distributed Computing",
]

dependencies = []

[project.urls]
Homepage = "https://github.com/jhuang-tw/djobs"
Repository = "https://github.com/jhuang-tw/djobs"
Issues = "https://github.com/jhuang-tw/djobs/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
pg = [
    "psycopg[binary]>=3.1",
]
mcp = [
    "mcp[cli]>=1.0",
]

[project.scripts]
djobs = "djobs.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/djobs"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
src = ["src", "tests"]
target-version = "py311"
line-length = 99

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]
